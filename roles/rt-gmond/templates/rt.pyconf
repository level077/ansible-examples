modules {
    module {
        name = "rt_{{ port }}"
        language = "python"
  }
}

collection_group {
    collect_every = 10 
    time_threshold = 20
    metric {
        name="rt_{{ port }}"
        title="{{ port }} RT"
	value_threshold = "1.0"
  }
}
